  // unique_ptr<int> ptr(new int(42));
    // // unique_ptr<int> cp = ptr; //ERROR, can't copy unique_ptr

    // unique_ptr<int> ptr2(new int(5));
    // // ptr = ptr2; //ERROR, can't assign unique_ptr
    // cout << (*ptr) << endl;